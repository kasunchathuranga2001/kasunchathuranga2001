package com.smartcampus.enums;

/**
 * Ticket status workflow: OPEN → IN_PROGRESS → RESOLVED → CLOSED (or REJECTED)
 */
public enum TicketStatus {
    OPEN,
    IN_PROGRESS,
    RESOLVED,
    CLOSED,
    REJECTED
}
